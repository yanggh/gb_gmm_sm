cmake_minimum_required (VERSION 2.6)
include_directories("${PROJECT_SOURCE_DIR}/")
include_directories("../../include")
link_directories(../../lib)
#aux_source_directory(. DIR_LIB_SRCS)
FILE(GLOB SRC_LIST ${CMAKE_CURRENT_SOURCE_DIR}/nbe_pu*.c ${CMAKE_CURRENT_SOURCE_DIR}/nbp_*.c)
FOREACH(DIR_LIB_SRCS ${SRC_LIST})
  MESSAGE( ${DIR_LIB_SRCS} )
ENDFOREACH(DIR_LIB_SRCS ${SRC_LIST})
add_library (gb_proto SHARED ${SRC_LIST})
target_link_libraries(gb_proto -lnbe  -lrt -ldl -lanl -lpthread)
